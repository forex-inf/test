## For use installing lib  python-gitlab v4.1.1

    pip install python-gitlab

## For use set variable in blk_users.py

    HOST - ip or domain gitlab server`s (string)
    TOKEN - auth private token gitlab user (string)
